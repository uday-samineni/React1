import React from "react";
import Svg, { Circle, G, Path } from "react-native-svg";

const Reject = props => (
  <Svg data-name="Group 2227" width={24} height={24} {...props}>
    <Circle data-name="Ellipse 323" cx={12} cy={12} r={12} fill="red" />
  </Svg>
);

export default Reject;
